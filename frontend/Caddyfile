{
	admin :2020
}

:{$LISTEN_PORT} {
	log {
		level debug
	}

	# API 反向代理
	handle_path {$LISTEN_PREFIX}/api/* {
		reverse_proxy localhost:{$BACKEND_PORT}
	}

	# 静态网站 (SPA)
	handle_path {$LISTEN_PREFIX}/* {
		root * ./dist
		try_files {path} /index.html
		file_server
	}
}